{
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "start:db": "json-server --watch db.json",
    "start:parcel": "parcel index.html",
    "dev": "concurrently \"npm run start:parcel\" \"npm run start:db\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "json-server": "^0.17.3",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "axios": "^1.4.0"
  },
  "proxy": "http://localhost:3000"
}
